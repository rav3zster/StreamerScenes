## Plan: Fix All Non-Functional / Broken Features

**6 fixes already applied** (from prior session):
- ✅ Scene CRUD (create/duplicate/delete) in LeftPanel + `duplicateScene` store action
- ✅ Scene transition dropdown wired to `liveTransitionType` store
- ✅ Glass blur slider now reads `s.blur` from widget style (was hardcoded 12px)
- ✅ Dead `···` menu buttons removed from LeftPanel (3 instances)
- ✅ CommandPalette theme toggle syncs `editorTheme` store + `.app-shell` class
- ✅ `duplicateScene` action added to editorStore

**8 remaining fixes to implement:**

### 1. Add Undo/Redo buttons to TopToolbar (HIGH)
**File:** `src/editor/toolbar/TopToolbar.tsx`
- `canUndo`/`canRedo` are destructured at line 17 but never rendered. They are **functions** returning booleans, so must call them: `canUndo()` / `canRedo()`
- Add undo/redo icon buttons between the theme toggle and Preview pill in the right action bar
- Wire `onClick` to `undo()` / `redo()` from the store
- Disable buttons when `canUndo()` or `canRedo()` returns false

### 2. Wire RightPanel property search to filter sections (MEDIUM)
**File:** `src/editor/panels/RightPanel.tsx`
- `propertySearch` state (line 302) is set but never read
- Modify `InspectorSection` component (line 652) to accept an optional `searchQuery` prop
- When `searchQuery` is non-empty, hide sections whose `title` doesn't match, and auto-expand matching sections
- Pass `propertySearch` down from `SingleInspector` to each `InspectorSection`

### 3. Wire RightPanel background fill controls to real state (MEDIUM)
**File:** `src/editor/panels/RightPanel.tsx`
- `CanvasPropertiesInspector` (line 23): `fillMode` and `hexColor` are local-only state, never written to any store
- Replace local state with reading/writing the editing scene's first `background` widget's style, OR add a `sceneBackgroundColor` field to the Scene type
- Simplest approach: find the `background`-type widget in the editing scene, read its `style.background`, and update it via `updateWidget`
- Fix typo `'glace'` → `'glass'` in the segmented control
- Wire the color swatch div (line 87-91) with an `onClick` that opens color input

### 4. Remove dead ··· buttons in RightPanel (LOW)
**File:** `src/editor/panels/RightPanel.tsx`
- Three decorative `···` buttons at lines 50, 107, 149 with no `onClick`
- Simply remove them — they're section headers that don't need menus

### 5. Make scene ID chip dynamic (LOW)
**File:** `src/editor/panels/RightPanel.tsx`
- Line 66: hardcoded `#N56675` — replace with actual editing scene ID, truncated/hashed for display
- Derive from `editingSceneId` prop

### 6. Persist asset favorites to store (LOW)
**File:** `src/editor/panels/LeftPanel.tsx`
- `favAssets` in `AssetsTab` (line 568) is local `useState` — resets on tab switch
- Add `favoriteAssetIds: string[]` and `toggleFavoriteAsset(id: string)` to the editor store
- Read/write from store instead of local state

### 7. Add Images category assets to MOCK_ASSETS (LOW)
**File:** `src/data/assets.ts`
- The "Images" category filter shows an empty grid because `MOCK_ASSETS` has zero `category: 'Images'` entries
- Add 2-3 image-type entries with placeholder URLs (e.g., avatar placeholders, overlay frames)

### 8. Build and verify (HIGH)
- Run `npx tsc -b` to ensure all changes compile
- Run `npx vite build` for production verification
- Fix any TypeScript errors that arise

**Files modified:** 5 files total
- `src/editor/toolbar/TopToolbar.tsx` — undo/redo buttons
- `src/editor/panels/RightPanel.tsx` — property search, background fill, ··· buttons, scene ID
- `src/editor/panels/LeftPanel.tsx` — asset favorites persistence
- `src/store/editorStore.ts` — add `favoriteAssetIds` state + `toggleFavoriteAsset` action
- `src/data/assets.ts` — add Images category entries